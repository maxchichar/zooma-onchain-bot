# Secrets - never commit real values. .env.example (with blank values)
# is the one that belongs in the repo.
.env
.env.*
!.env.example

node_modules/

# Build output, logs, editor/OS cruft
dist/
*.log
.DS_Store

# Runtime state stores
.paper_wallet_store.json
.sniper_state.json
.active_chats.json